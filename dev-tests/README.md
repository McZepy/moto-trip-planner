# Test manuali MotoRoute

Questa cartella contiene le pagine HTML usate per i collaudi manuali durante lo sviluppo.

I file TypeScript dei test restano in `src/dev` perche' sono utili come regressione tecnica.

Le vecchie prove browser/proxy Overpass sono state rimosse: il catalogo OSM viene ora generato fuori dal browser e caricato come dato statico/runtime.
